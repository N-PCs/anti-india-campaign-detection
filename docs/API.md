# Anti-India Campaign Detection System API Documentation

## Base URL
`http://localhost:5000` (development)
`https://your-domain.com` (production)

## Authentication
Currently, the API uses a simple token-based authentication. Include the token in the Authorization header:
